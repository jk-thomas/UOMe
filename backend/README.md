# Backend (NodeJS + Express + SQLite)

## Setup
1. Copy .env.example to .env and edit as needed
2. Install deps: npm install
3. Run: npm run dev

## Endpoints
- GET /api/health
- GET /api/expenses
- POST /api/expenses
- GET /api/settlement
